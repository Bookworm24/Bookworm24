import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
c = np.vstack((a, b))
print("c:\n", c)

"""
在上述代码中，`c` 是通过垂直堆叠（Vertical Stack）操作将数组 `a` 和 `b` 连接在一起得到的结果。

`np.vstack` 是 NumPy 库中的一个函数，用于将多个数组垂直堆叠在一起。在这个例子中，我们使用 `np.vstack((a, b))` 将数组 `a` 和 `b` 垂直堆叠在一起。

具体而言，`np.vstack((a, b))` 的结果是一个新的数组 `c`，其中 `a` 是位于上方，`b` 是位于下方。堆叠时，要求 `a` 和 `b` 的列数必须相同。

例如，如果 `a` 是一个 2x3 的数组，`b` 是一个 2x3 的数组，那么 `c` 将是一个 4x3 的数组，其中前两行是来自 `a`，后两行是来自 `b`。

需要注意的是，`np.vstack` 操作不会改变原始数组 `a` 和 `b`，而是生成一个新的数组 `c`。

总而言之，通过执行 `c = np.vstack((a, b))`，我们可以将数组 `a` 和 `b` 垂直堆叠在一起得到一个新的数组 `c`。这可以用于合并多个数组或在垂直方向上扩展数组的维度。
"""